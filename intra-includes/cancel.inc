<!-- This is currently not in use: the cancel method is handled in
     ill-manage.{pp,tt} by simply checking whether we cancelled successfully
     and then passing a 'message' to the template.

     This approach means backends cannot currently use 'stages' in the cancel
     method call. -->
